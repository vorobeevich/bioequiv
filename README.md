# БиоЭкв — автоматизация дизайна исследований биоэквивалентности

> МНН → оригинальный препарат → ФК параметры из 9 источников → синопсис протокола (Word)

**Демо:** http://89.167.40.65:8501 (доступно только через VPN)

---

## Архитектура

```
МНН + лекарственная форма (ввод пользователя)
         │
         ▼
┌─ Стадия 1: МНН → оригинальный препарат ─────────────────────┐
│  ЕАЭС реестр (8 000+ записей)                                │
│  exact match → fuzzy (rapidfuzz) → LLM-валидация              │
│  + фильтрация по лекарственной форме                         │
└─────────────────────────┬────────────────────────────────────┘
                          ▼
┌─ Стадия 2: сбор ФК параметров ───────────────────────────────┐
│                                                                │
│  2.0  Видаль (препараты) — 10 500+, ФК + противопоказания     │
│  2.1  Видаль (вещества) — 4 600+, мост рус↔лат               │
│  2.2  Числовые базы: e-Drug3D (2 162), OSP (340+)            │
│  2.2b CVintra/PMC — 53 вещества (Park et al. 2020)           │
│  2.3  ОХЛП — 11 000+ записей, 15 разделов PDF                │
│  2.3b FDA PSG — 1 613 руководств FDA по дизайну BE           │
│  2.4  DrugBank — 19 830 веществ (текст, англ.)               │
│  2.5  LLM-валидация всех fuzzy-матчей                         │
│  2.6  LLM анализирует ВСЕ данные → выбирает лучшее           │
│                                                                │
│  Приоритет: препарат > вещество/числа > вещество/текст       │
│  Результат: Cmax, AUC, Tmax, T½, CVintra + источник + цитата │
└─────────────────────────┬────────────────────────────────────┘
                          ▼
┌─ Стадия 3: генерация синопсиса протокола ────────────────────┐
│                                                                │
│  Программные расчёты (~20 полей):                             │
│    Дизайн (2×2 / replicated / parallel)                       │
│    Размер выборки (Power TOST, scipy)                         │
│    RSABE (если CVintra > 30%)                                 │
│    График крови (15–22 точки до 72 ч)                         │
│    Отмывочный (5×T½), периоды, длительность                  │
│    Критерии БЭ, стат. методы, этика, рандомизация            │
│                                                                │
│  3 LLM-вызова:                                                │
│    1. Задачи + дизайн (кратность дозы из ОХЛП)               │
│    2. Критерии включения/невключения (ОХЛП + Rule 85)        │
│    3. Описания препаратов + безопасность                      │
│                                                                │
│  Выход: 29 полей → Word (.docx) + предпросмотр в UI          │
└──────────────────────────────────────────────────────────────┘
```

---

## Целевые ФК параметры

| Параметр | Единица | Назначение |
|----------|---------|------------|
| **Cmax** | нг/мл | Первичная конечная точка БЭ |
| **AUC** | нг*ч/мл | Первичная конечная точка БЭ |
| **Tmax** | ч | График крови; критерий рвоты (2×Tmax) |
| **T½** | ч | Отмывочный период (≥5×T½); kel |
| **CVintra** | % | Размер выборки; HVD статус (≥30%) |

---

## Источники данных

| # | Источник | Записей | Уровень | Что даёт |
|---|----------|---------|---------|----------|
| 1 | Реестр ЕАЭС | 8 000+ | — | МНН → оригинальный препарат |
| 2 | Видаль (препараты) | 10 500+ | препарат | ФК, фармакология, противопоказания |
| 3 | Видаль (вещества) | 4 600+ | вещество | ФК текст, мост рус↔лат |
| 4 | e-Drug3D | 2 162 | вещество | Cmax, Tmax, T½ |
| 5 | OSP | 340+ | вещество | AUC, Cmax, CV% |
| 6 | CVintra/PMC | 53 | вещество | CVintra Cmax/AUC (Park et al. 2020) |
| 7 | DrugBank | 19 830 | вещество | Absorption, half-life, clearance |
| 8 | ОХЛП PDF | 11 000+ | препарат/вещество | 15 разделов (ФК, дозирование, противопоказания) |
| 9 | FDA PSG | 1 613 | вещество | Дизайн BE, CVintra-порог, NTI/HVD |

---

## Данные

Данные (~585 MB) не входят в репозиторий. Скачайте с Яндекс.Диска и распакуйте в папку `data/`:

**[Скачать данные с Яндекс.Диска](https://disk.360.yandex.ru/d/MCD1W5t7eObHfg)**

После скачивания структура `data/` должна быть:
```
data/
├── eaeu_registry.csv        3.2 MB
├── vidal_molecules.csv       15 MB
├── vidal_drugs_merged.csv    86 MB
├── edrug3d_pk.csv           208 KB
├── osp_pk_parameters.csv    339 KB
├── cvintra_pmc.csv            5 KB
├── drugbank_pk.csv           18 MB
├── ohlp_pk_texts.csv        460 MB
└── fda_psg_parsed.csv       1.9 MB
```

---

## User Guide — пошаговая инструкция

Ниже — использование приложения на примере **аторвастатина** (таблетки, покрытые плёночной оболочкой).

### Стадия 1 — ввод МНН

1. В поле **МНН** введите `аторвастатин` (или другой МНН).
2. В поле **Форма** (необязательно) укажите `таблетки, покрытые плёночной оболочкой` — для точного матча в реестре ЕАЭС.
3. Нажмите **🔍 Найти**.

Система найдёт оригинальный препарат (Липримар®, Pfizer/Виатрис), тип матча (exact/fuzzy), АТХ-код и торговые названия.

### Стадия 2 — сбор ФК параметров

Выполняется автоматически после Стадии 1. Источники опрашиваются по очереди:

- Видаль (препарат + вещество)
- e-Drug3D, OSP, CVintra/PMC
- ОХЛП, FDA PSG, DrugBank

Итог: таблица параметров (Cmax, AUC, Tmax, T½, CVintra) с источниками. При fuzzy-матчах включается LLM-валидация (если задан API-ключ).

### Стадия 3 — генерация синопсиса

Заполните форму и нажмите **📋 Сгенерировать синопсис**.

**Обязательные и основные поля:**

| Поле | Описание | Пример |
|------|----------|--------|
| Исследуемый препарат (генерик) | Название тест-препарата | `Аторвастатин-Тест` |
| Лекарственная форма | Подставляется из FDA PSG/Видаль | `таблетки, покрытые плёночной оболочкой` |
| Дозировка | Сила препарата | `40 мг` |
| Спонсор исследования | Организация-спонсор | `ООО «Фарма», Россия` |

**Режим приёма:** авто / натощак / после еды / оба.  
**Дизайн:** авто (2×2 / replicated / parallel по CVintra и FDA PSG).  
**Кратность дозы:** однократная / многократная (LLM может подсказать по ОХЛП).  
**Пол добровольцев:** мужчины и женщины / только мужчины / только женщины.

**Дополнительно:**

| Поле | Описание |
|------|----------|
| CVintra (%) | 0 = авто из баз; иначе подставляется в расчёты |
| Возрастной диапазон | По умолчанию `18-45` |
| Использовать RSABE | Для HVD (CVintra ≥30%) — расширенные границы Cmax |
| Дополнительные требования | Произвольный текст для LLM |

Результат: 29 полей синопсиса, предпросмотр в UI, экспорт в Word (.docx).

---

## Быстрый старт

```bash
git clone https://github.com/YOUR_USER/bioequiv.git
cd bioequiv

# Скачать данные → положить CSV в data/
# https://disk.360.yandex.ru/d/MCD1W5t7eObHfg

python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# API ключ DeepSeek (без него работает, но без LLM-генерации)
echo "DEEPSEEK_API_KEY=sk-..." > .env

# Тесты
python -m pytest tests/ -v

# Запуск
streamlit run "1_💊_Анализ.py"
```

---

## Деплой на сервер

Подробная инструкция: **[DEPLOY.md](DEPLOY.md)**

---

## Структура проекта

```
├── 1_💊_Анализ.py              Streamlit — главная страница
├── pages/
│   └── 2_📐_Схема_работы.py    Страница «Схема работы»
├── run.py                      CLI: python run.py --inn "МНН"
│
├── pipeline/                   Ядро
│   ├── config.py               Пути к данным, API ключ
│   ├── models.py               DrugInfo, PKValue, PKParams
│   ├── stage1.py               МНН → оригинал (реестр + LLM fuzzy)
│   ├── stage2.py               Оркестратор: 9 источников → LLM → 5 параметров
│   ├── stage3.py               Синопсис: расчёты + 3 LLM → 29 полей → .docx
│   ├── sample_size.py          Power TOST (scipy)
│   ├── timepoints.py           График отбора крови
│   └── stage2_sources/
│       ├── vidal.py            Видаль (препараты + вещества)
│       ├── edrug3d.py          e-Drug3D
│       ├── osp.py              OSP
│       ├── cvintra_pmc.py      CVintra/PMC
│       ├── drugbank.py         DrugBank
│       ├── ohlp.py             ОХЛП (15 разделов)
│       ├── fda_psg.py          FDA PSG
│       └── llm_extract.py      LLM: экстракция ФК + валидация fuzzy
│
├── tests/
│   └── test_pipeline.py        Тесты пайплайна
│
├── docs/
│   ├── rule85_context.md       Ключевые пункты Решения ЕАЭК №85
│   ├── stage3_design.md        Архитектура Стадии 3
│   ├── synopsis_template.md    Шаблон синопсиса
│   └── why_deepseek.md         Обоснование выбора LLM
│
├── assets/banner.png
├── deploy.sh                   Деплой на сервер
├── requirements.txt
├── DEPLOY.md
└── DATA.md                     Описание файлов данных
```

---

## Тесты

**110 тестов** в `tests/test_pipeline.py`, покрывают все три стадии пайплайна, каждый источник данных и целостность баз.

```bash
python -m pytest tests/ -v          # pytest
python tests/test_pipeline.py       # встроенный runner с группировкой
```

### Статистика по группам

| Группа | Кол-во | Что проверяет |
|--------|--------|---------------|
| **Stage 1 — реестр ЕАЭС** | 8 | exact/fuzzy/not-found поиск МНН, `find_original`, мультитипы (оригинал + генерик), АТХ-код, торговые названия |
| **Видаль — вещества** | 6 | exact/latin поиск, наличие ФК текста, мост рус↔лат, not-found, отсеивание ложных fuzzy |
| **Видаль — препараты** | 3 | поиск по торговому названию, `level=drug`, anti-regression сертралин vs Золототысячник |
| **e-Drug3D** | 5 | T½ амлодипина (~40 ч) и ибупрофена (~2 ч), not-found, `match_type`, все параметры — `PKValue` |
| **OSP** | 4 | CVintra верапамила, мидазолам, not-found, валидация диапазона CVintra |
| **CVintra/PMC** | 5 | амлодипин, аторвастатин (HVD ≥30%), размер выборки, not-found, `PKValue` с unit=% |
| **DrugBank** | 5 | ибупрофен, URL drugbank.org, half_life текст, not-found, `drugbank_id` формат DB* |
| **ОХЛП** | 6 | поиск по МНН, приоритет по торговому названию, level drug/substance, разделы (ФК/дозирование/противопоказания), not-found |
| **FDA PSG** | 13 | существование файла, 1000+ записей, поиск амлодипина, NTI карбамазепин, replicated карбамазепин, HVD акампросат, CVintra-порог 30, дизайн натощак, PDF URL, not-found, обязательные поля, `search_all` 2+ записей, интеграция в Stage 2 |
| **Stage 2 — полный пайплайн** | 12 | структура лога `[2.0]`/`[2.1]`/`[2.2]`, T½ амлодипина из e-Drug3D, 3 источника ибупрофена (Видаль + e-Drug3D + DrugBank), T½ ибупрофена, CVintra PMC, интеграция ОХЛП, уровень ОХЛП в логе, `PKParams` тип, вилдаглиптин, метформин, anti-regression сертралин, фильтр молярных единиц Cmax |
| **Data integrity** | 7 | ЕАЭС ≥1000, Видаль ≥1000 (≥500 с латиницей), e-Drug3D ≥500, OSP ≥100, CVintra PMC ≥50, DrugBank ≥1000, ОХЛП ≥1000 |
| **Stage 3 — расчёты** | 33 | sample size (basic/NTI/high-CV/replicated/formula note), determine_design (standard/HVD/NTI), timepoints (basic/long-T½/blood volume/schedule text), `compute_derived` (полный/без T½/с FDA flags), `generate_docx` (минимальный/полный), `generate_synopsis` без LLM (с CV/без CV/без ФК), `_parse_llm_json` (clean/markdown/preamble/invalid), `collect_all_data` (полный/пустой), LLM_CALLS mapping (3 вызова), промпты для всех 3 вызовов, контекст противопоказаний в промпте criteria, `generate_synopsis_step` с мок-LLM, `_extract_rule85_sections` |
| **LLM (требуют API ключ)** | 6 | Stage 2 ибупрофен ≥2 параметра, амлодипин ≥3 параметра, source_tag в PKValue, Stage 3 полный пайплайн с LLM (3 вызова, 19 секций) |
| **Итого** | **110** | |

### Без API ключа

Тесты в группе «LLM» автоматически пропускаются (SKIP), если переменная `DEEPSEEK_API_KEY` не задана. Все остальные 104 теста работают полностью офлайн.

---

## Стек

| Компонент | Технология |
|-----------|------------|
| Язык | Python 3.9+ |
| UI | Streamlit |
| Матчинг | rapidfuzz |
| LLM | DeepSeek API (`deepseek-chat`) |
| Расчёт выборки | scipy (нецентральное t) |
| Генерация docx | python-docx |

---

## Тестовый пример

**Аторвастатин**:

```
МНН:       аторвастатин
Форма:     таблетки, покрытые плёночной оболочкой
Оригинал:  Липримар® (Виатрис Спешиалти ЭлЭлСи)

Cmax     = 15.8 нг/мл   (OSP)
AUC      = 62.5 нг*ч/мл (OSP)
Tmax     = 1.5 ч         (e-Drug3D)
T½       = 14.0 ч        (e-Drug3D + DrugBank)
CVintra  = 31.6%         (PMC6989220)

→ Дизайн: replicated (CV > 30%, HVD по FDA)
→ RSABE для Cmax: [79.10%; 126.43%]
→ Выборка: ~44 для анализа, ~52 с dropout, ~66 скрининг
→ Отмывочный: 3 дня (5 × 14 ч)
```

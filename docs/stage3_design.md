# Стадия 3 — Генерация синопсиса протокола (архитектура v3)

## Обзор

Стадия 3 принимает результаты Stage 1 (оригинальный препарат из реестра ЕАЭС) и Stage 2 (ФК параметры из 7+ баз данных) и генерирует полный синопсис протокола исследования биоэквивалентности в формате Word (.docx).

**Ключевой принцип:** максимум программных шаблонов (быстро, детерминированно), минимум LLM-вызовов (только там, где нужен анализ данных конкретного препарата).

- **~20 полей** генерируются **программно** (шаблоны из instructions.docx + формулы)
- **3 LLM-вызова** (вместо 6 в v1) — дизайн+задачи, критерии, описания препаратов

---

## Потоки данных

```
                ┌─────────────────────────────────┐
                │         Пользовательский ввод    │
                │  test_drug_name, dosage_form,    │
                │  strength, fasting_fed, gender,  │
                │  cv_intra_user, design_preference│
                │  use_rsabe, study_phases, ...    │
                └───────────────┬─────────────────┘
                                │
         ┌──────────────────────┼───────────────────────┐
         │                      │                        │
         ▼                      ▼                        ▼
  ┌──────────────┐   ┌──────────────────┐   ┌───────────────────┐
  │   Stage 1    │   │     Stage 2      │   │   Rule 85 (.md)   │
  │  DrugInfo    │   │  Stage2Result    │   │  Нормативная база │
  │  (реестр)    │   │  (ФК параметры)  │   │                   │
  └──────┬───────┘   └────────┬─────────┘   └─────────┬─────────┘
         │                    │                        │
         └────────────┬───────┘                        │
                      ▼                                │
              ┌───────────────┐                        │
              │ Stage3Input   │                        │
              │ (все входы)   │                        │
              └───────┬───────┘                        │
                      │                                │
                      ▼                                │
              ┌───────────────┐                        │
              │compute_derived│                        │
              │  (расчёты)    │                        │
              └───────┬───────┘                        │
                      │                                │
           ┌──────────┼──────────┐                     │
           ▼                     ▼                     ▼
  ┌────────────────┐   ┌────────────────┐   ┌──────────────────┐
  │  ПРОГРАММНЫЕ   │   │  3 LLM-ВЫЗОВА │   │ collect_all_data │
  │  ШАБЛОНЫ       │   │               │   │  (данные из S2)  │
  │  (~20 полей)   │   │  criteria +   │   │                  │
  │                │   │  drug_safety   │   │                  │
  └───────┬────────┘   └───────┬────────┘   └──────────────────┘
          │                    │
          └────────┬───────────┘
                   ▼
           ┌───────────────┐
           │   СИНОПСИС    │
           │  (dict полей) │
           └───────┬───────┘
                   ▼
           ┌───────────────┐
           │ generate_docx │
           │  (Word .docx) │
           └───────────────┘
```

---

## Входные параметры (Stage3Input)

| Параметр | Тип | Описание | По умолчанию |
|---|---|---|---|
| `drug_info` | DrugInfo | Оригинальный препарат из Stage 1 | — |
| `s2` | Stage2Result | Все данные Stage 2 (ФК, ОХЛП, Видаль, FDA PSG...) | — |
| `test_drug_name` | str | Название генерика (вводит пользователь) | "" |
| `dosage_form` | str | Лекарственная форма | "" |
| `strength` | str | Дозировка (напр. "400 мг") | "" |
| `sponsor` | str | Спонсор | "ХХХХХ, Россия" |
| `fasting_fed` | str | Режим: "fasting" / "fed" / "both" / "" (авто) | "" |
| `cv_intra_user` | float | CVintra от пользователя (0 = из Stage 2) | 0.0 |
| `use_rsabe` | bool | Принудительно RSABE для Cmax | False |
| `design_preference` | str | Дизайн: "2x2" / "replicated" / "parallel" / "" | "" |
| `study_phases` | str | "single" / "multiple" | "single" |
| `gender` | str | "both" / "male" / "female" | "both" |
| `age_range` | str | Возрастной диапазон | "18-45" |
| `additional_requirements` | str | Свободный текст с доп. требованиями | "" |

---

## Вычисляемые значения (compute_derived)

На основе входных данных вычисляются:

| Значение | Формула / логика |
|---|---|
| **cv_intra** | `cv_intra_user` > 0 → user; иначе → Stage2 PK → FDA PSG threshold |
| **washout_days** | ⌈5 × T½ / 24⌉ дней |
| **vomit_criterion_h** | 2 × Tmax часов |
| **design** | Из preference → из CVintra/HVD/NTI → default 2x2 |
| **sample_size** | PowerTOST (scipy nct / normal approx) |
| **n_to_screen** | ⌈n_total / 0.80⌉ (20% скрин-фейл) |
| **timepoints** | Алгоритм из timepoints.py (Tmax + T½ → оптимальный график) |
| **fasting_text** | Из user preference → из FDA PSG → default "натощак" |
| **RSABE limits** | При CVintra > 30% + replicated: exp(±k·swR), k=0.760 |
| **study_duration_days** | 14 + n_periods × pk_days + n_washouts × washout + 7 |

---

## Программные поля (20+ полей)

Генерируются функцией `generate_programmatic_fields()` без вызова LLM:

| # | Поле | Метод | Источник |
|---|---|---|---|
| 1 | protocol_title | Шаблон | Все вычисленные значения |
| 2 | protocol_id | `{МНН}-BE` | Stage 1 |
| 3 | sponsor | Ввод пользователя | User / default |
| 4 | phase | Константа | "Исследование биоэквивалентности" |
| 5 | study_objectives | Шаблон | test_drug, ref_drug, fasting |
| 6 | tasks | Шаблон | INN, dose_type |
| 7 | study_design | Шаблон + rationale | Design, T½, washout |
| 8 | methodology | Сложный шаблон | Все computed: periods, groups, sampling |
| 9 | sample_size_text | Шаблон + формула | CV, PowerTOST, dropout, screen fail |
| 10 | study_periods | Шаблон с датами | Visit schedule, days |
| 11 | study_duration | Шаблон | study_duration_days |
| 12 | pk_parameters | Шаблон | INN |
| 13 | analytical_method | Шаблон | INN |
| 14 | be_criteria | Шаблон + RSABE | CV, k, swR (если RSABE) |
| 15 | sample_size_calculation | Формула + пояснения | CV, σ²_w, δ, N |
| 16 | statistical_methods | Шаблон + RSABE | INN, ANOVA model |
| 17 | blinding_randomization | Шаблон | Groups, sequences |
| 18 | ethical_aspects | Константа | Sponsor |
| 19 | protocol_version | `1.0 от {дата}` | today() |

---

## LLM-вызовы (3 штуки)

### CALL 1: `study_design_analysis`
**Поля:** tasks, study_design

**Входные данные:**
- ОХЛП: способ применения и дозы (dosing_text), фармакокинетика (pk_text), лекарственная форма (form_text)
- Видаль: данные о препарате, ФК вещества
- FDA PSG: дизайн (fasting/fed), комментарии, дозировка
- Computed: design_details, t_half, cv_intra, washout_days
- Rule 85: п. 15–16 (Дизайн)

**Зачем LLM:**
- Определить кратность дозирования (однократное/многократное) по данным ОХЛП:
  - Многократное нужно ТОЛЬКО при: непереносимости однократной дозы, невозможности зафиксировать концентрацию, модифицированном высвобождении
- Обосновать выбранный дизайн исследования с учётом специфики препарата
- Дополнить программный текст клинической аргументацией

**Механизм:** LLM получает программно сгенерированные тексты задач и дизайна как «базу», и улучшает/дополняет их на основе данных ОХЛП и FDA PSG.

### CALL 2: `criteria`
**Поля:** inclusion_criteria, exclusion_criteria, withdrawal_criteria

**Входные данные:**
- ОХЛП: противопоказания, особые указания, беременность, взаимодействия, НЯ, показания
- Видаль: противопоказания вещества, показания
- Computed: gender, age_range, vomit_criterion_h
- Rule 85: п. 26–31 (Субъекты)

**Зачем LLM:**
- Нужно адаптировать шаблонные критерии к конкретному препарату
- Убрать/добавить пол в зависимости от показаний
- Добавить специфические противопоказания из ОХЛП
- Рассчитать критерий рвоты

### CALL 3: `drug_safety`
**Поля:** test_drug_details, reference_drug_details, safety_analysis

**Входные данные:**
- ОХЛП: состав, форма, вспомогательные вещества, хранение, срок годности, НЯ, передозировка
- Видаль: данные о препарате (ФК, состав)
- DrugBank: метаболизм, элиминация
- FDA PSG: дозировка

**Зачем LLM:**
- Нужно синтезировать данные из нескольких источников
- Заполнить структурированную карточку препарата
- Определить специфические анализы безопасности

---

## RSABE (Reference-Scaled Average Bioequivalence)

Применяется когда:
1. Дизайн replicated (3- или 4-way)
2. CVwR (within-subject CV референта) > 30%
3. Или явно запрошено пользователем

Формулы:
```
swR = √(ln(1 + CV²))     где CV = CVintra/100
k = 0.760
Границы Cmax: [exp(-k·swR) × 100% ; exp(k·swR) × 100%]
```

При CV = 30%: границы = 80.00–125.02% (≈ стандартные)
При CV = 45%: границы = 72.15–138.59%

AUC всегда оценивается по стандартным границам 80–125%.

---

## Расчёт выборки

```
σ²_w = ln(1 + CV²)
δ = ln(θ)                   θ = 1.25 (стандарт) или 1.1111 (NTI)
N_eval = 2·(Z_α + Z_β)²·σ²_w / δ²    (normal approx)

N_total = ⌈N_eval / (1 - 0.15)⌉       (dropout 15%)
N_screen = ⌈N_total / (1 - 0.20)⌉     (screen fail 20%)
```

Минимум: 12 добровольцев (Правило 85, п. 26).

---

## Пример: Амлодипин 10 мг

### Входные данные
```
МНН: амлодипин
Оригинальный препарат: Норваск® (Viatris Specialty LLC)
Исследуемый: Амлодипин-Тест, таблетки, 10 мг
Спонсор: ООО "ФармаТест", Россия

ФК параметры (из Stage 2):
  Cmax = 6.0 нг/мл (e-Drug3D)
  AUC = 200.0 нг*ч/мл (e-Drug3D)
  Tmax = 6.0 ч (Видаль)
  T½ = 35.0 ч (DrugBank)
  CVintra = 25.0% (CVintra/PMC)

Параметры Stage 3:
  Режим: натощак
  Пол: мужчины и женщины
  Возраст: 18-45 лет
  Доза: однократная
```

### Вычисленные значения
```
Дизайн: 2x2 (CVintra < 30%)
Отмывочный: ⌈5 × 35 / 24⌉ = 8 дней
Критерий рвоты: 2 × 6.0 = 12.0 ч
Выборка: 20 доб. (10 на гр.), скринировать до 26
Продолжительность: ~39 дней
График крови: 0 (до приёма), 30 мин, 45 мин, 1 ч, ... 72 ч
RSABE: не применяется (CV < 30%)
```

### Результат
```
Идентификатор: амлодипин-BE
Название: Открытое рандомизированное двухпериодное перекрёстное исследование
  сравнительной фармакокинетики (биоэквивалентности) препаратов
  Амлодипин-Тест, таблетки, 10 мг (ООО "ФармаТест", Россия)
  и референтного препарата Норваск® (Viatris Specialty LLC)
  натощак у здоровых добровольцев.
Версия: 1.0 от 25 февраля 2026 года
```

---

## Пример: Высоковариабельный препарат (CVintra = 45%, RSABE)

### Отличия от стандартного
```
Дизайн: replicated (4-period, TRTR/RTRT)
RSABE для Cmax:
  swR = √ln(1 + 0.45²) = 0.4294
  Границы: exp(±0.76 × 0.4294) = [72.15%; 138.59%]
AUC: стандартные 80–125%
```

---

## Файлы

| Файл | Описание |
|---|---|
| `pipeline/stage3.py` | Основная логика Stage 3 |
| `pipeline/sample_size.py` | Расчёт выборки (PowerTOST) |
| `pipeline/timepoints.py` | Генерация графика крови |
| `pipeline/models.py` | DrugInfo, PKParams |
| `pipeline/stage2.py` | Stage2Result |
| `docs/rule85_context.md` | Выжимка из Правила 85 |
| `docs/instructions.docx` | Шаблоны и инструкции для каждого поля |
| `1_💊_Анализ.py` | Streamlit UI (все 3 стадии) |
